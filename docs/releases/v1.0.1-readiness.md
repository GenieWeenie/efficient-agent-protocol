# v1.0.1 Readiness Evidence

Status: Dry-run passed; ready to tag after EAP-133 merge and green post-merge workflows
Last updated: 2026-04-29  
Pre-dry-run baseline commit: `4ab719fdd13accd9b211e2f88bfea2a4df477611`

## Purpose

This document centralizes the evidence needed before cutting the `v1.0.1` patch release. It points to the hardening gates, CI proof, known warnings, and remaining blockers so release decisions do not depend on scattered Linear comments or GitHub run history alone.

## Current Release Decision

`v1.0.1` passed the final local release readiness dry-run for `EAP-133` / `GEN-213`. The only remaining condition before tagging is procedural: merge the EAP-133 PR and verify post-merge `main` workflows remain green at the selected release commit.

The codebase is on the `1.0.1` package baseline, Phase 13 production hardening is complete, and Phase 14 has removed the known package/build and GitHub Actions runtime warning noise.

## Phase Completion Evidence

| Phase / Item | Status | Evidence |
| --- | --- | --- |
| Phase 13 production hardening | Complete | `docs/phase13_production_hardening_roadmap.md` and `scripts/production_hardening_gatepack.py` |
| `EAP-130` package license metadata cleanup | Complete | PR #104, merge commit `2d8cbfceaf8a8b3c2fa304db2853e78d7e9367aa` |
| `EAP-131` GitHub Actions runtime warning cleanup | Complete | PR #105, merge commit `208523e6497e9f3789ec28f8b7f5f77b1d11d63b`, `docs/github_actions_runtime_inventory.md` |
| `EAP-132` release evidence/docs refresh | Complete | This document plus docs index/runbook links |
| `EAP-133` final readiness dry-run | Complete | `scripts/v1_readiness_gatepack.py` passed 10/10; package build/install smoke passed |

## Post-Merge Workflow Evidence

Latest verified `main` baseline before EAP-133 merge: `4ab719fdd13accd9b211e2f88bfea2a4df477611`.

| Workflow | Run | Status |
| --- | --- | --- |
| CI | https://github.com/GenieWeenie/efficient-agent-protocol/actions/runs/25137724309 | Passed |
| Security | https://github.com/GenieWeenie/efficient-agent-protocol/actions/runs/25137724302 | Passed |
| CodeQL | https://github.com/GenieWeenie/efficient-agent-protocol/actions/runs/25137724321 | Passed |
| Release Drafter | https://github.com/GenieWeenie/efficient-agent-protocol/actions/runs/25137724314 | Passed |
| OpenClaw Interop | https://github.com/GenieWeenie/efficient-agent-protocol/actions/runs/25137724310 | Passed |

## Local Dry-Run Evidence

These commands passed during `EAP-133` and were attached to `GEN-213`:

```bash
PYTHONPATH=. python scripts/v1_readiness_gatepack.py
PYTHONPATH=. python scripts/production_hardening_gatepack.py
python -m pytest -q tests/contract/test_docs_v1_alignment.py tests/contract/test_release_evidence_contract.py
python -m build
```

Observed results:

- `scripts/v1_readiness_gatepack.py` reported `PASS (10/10 gates passed)`.
- `scripts/production_hardening_gatepack.py` reported `PASS (6/6 gates passed)`.
- Release evidence contract tests passed.
- Package build produced `efficient_agent_protocol-1.0.1` wheel/sdist without the setuptools license metadata deprecation warning.
- Isolated wheel install smoke reported version `1.0.1` and imported `eap`, `eap.agent`, `eap.environment`, and `eap.protocol`.

## Known Warnings and Blockers

| Item | Release impact | Status |
| --- | --- | --- |
| Package license metadata deprecation | Blocking if it returns | Resolved by `EAP-130`; covered by `tests/contract/test_package_metadata_contract.py` |
| GitHub Actions Node.js 20 runtime warning noise | Blocking if actionable warnings return | Resolved where upstream Node 24 actions exist; tracked in `docs/github_actions_runtime_inventory.md` |
| `actions/dependency-review-action@v4` still declares Node 20 | Non-blocking upstream blocker | PR-only, opt-in behind `ENABLE_DEPENDENCY_REVIEW`, forced through Node 24 compatibility path, documented in `docs/github_actions_runtime_inventory.md` |
| Final `v1.0.1` release dry-run | Blocking if it fails | Complete locally; post-merge workflow check remains required before tag |
| Local uv Python `ensurepip` abort during `pip-audit -r requirements.txt` | Non-blocking local tooling issue | `scripts/v1_readiness_gatepack.py` falls back to `pip-audit --local`; GitHub CI still enforces `pip-audit -r requirements.txt` successfully |

## Release Go/No-Go Rule

Go only when all of these are true:

- `EAP-133` PR is merged and Linear `GEN-213` is Done.
- Latest `main` CI, Security, CodeQL, Release Drafter, and OpenClaw Interop workflows are green.
- `scripts/v1_readiness_gatepack.py` and `scripts/production_hardening_gatepack.py` pass on the release baseline.
- `python -m build` completes without package metadata warnings.
- Any remaining workflow warnings are documented upstream blockers, not actionable repo-side fixes.

If any item fails, do not tag `v1.0.1`; create a new Linear issue in the active release-maintenance queue and link the failing evidence. If the EAP-133 PR merges and post-merge workflows stay green, `v1.0.1` is ready to tag.
