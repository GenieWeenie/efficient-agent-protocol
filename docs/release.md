# Release Runbook

This runbook defines a deterministic release process for EAP.

## Preconditions

- `main` is green in CI.
- Target changes are merged.
- Release draft notes are generated by Release Drafter.
- PyPI Trusted Publisher is configured for stable tag publishing.

## Semantic Version + Changelog Drafting

EAP uses GitHub Release Drafter to automate version resolution and changelog drafting.

Version resolution is label-based:

- `major` -> major bump
- `minor` -> minor bump
- `patch` -> patch bump
- default -> patch

The draft release body is the source of truth for release notes/changelog content.

Compatibility policy:

- If `docs/v1_contract_lock.json` changes, release version must be bumped in the same PR.
- CI enforces this via `scripts/check_v1_contract.py`.

## Release Steps

1. Ensure PR labels (`major`/`minor`/`patch`) are correct for merged changes.
2. Open the draft release in GitHub (`Releases` page).
3. Verify resolved version and notes.
4. Publish the release with tag `vX.Y.Z`.
5. Confirm release workflow success (`.github/workflows/release.yml`).
6. Verify published package(s) and attach release link in roadmap/issue tracking.
7. For releases with contract-surface changes:
   - confirm `docs/v1_contract_lock.json` was updated intentionally
   - confirm `Breaking Changes` and `Upgrade Notes` sections are populated

## V1.0 Release Checklist

Before tagging `v1.0.0`, complete every item below in order:

1. **Stabilization checklist**: verify all boxes in `docs/v1_stabilization_checklist.md` are checked.
2. **Readiness gatepack**: run `PYTHONPATH=. python scripts/v1_readiness_gatepack.py` and confirm `PASS (9/9)`.
3. **CI green**: all workflows on the release branch are green (CI, Security, CodeQL).
4. **Contract lock**: `docs/v1_contract_lock.json` reflects the intended v1.0 surface.
5. **Upgrade notes**: `docs/upgrade_notes_v1.md` is complete with breaking changes, migration actions, and rollback guidance.
6. **Release notes**: draft release body follows `docs/release_notes_template.md` structure (Added, Changed, Fixed, Deprecated, Removed, Breaking Changes, Upgrade Notes).
7. **Version bump**: `pyproject.toml` version is set to `1.0.0`.
8. **Owner sign-off**: all three sign-off lines in `docs/v1_stabilization_checklist.md` are approved.

### Pre-Release (RC) Tags

For release candidates before the final `v1.0.0` tag:

- Tag as `v1.0.0-rc1`, `v1.0.0-rc2`, etc.
- RC tags publish to TestPyPI (not production PyPI).
- Run the full gatepack against each RC to catch regressions.
- Promote to stable only after RC validation passes with no blockers.

## Publish Authentication

- Stable tags (`vX.Y.Z`) publish via PyPI Trusted Publishing (OIDC, no long-lived token).
- Pre-release tags (`vX.Y.Z-rcN`) use `TEST_PYPI_API_TOKEN` when present.

### PyPI Trusted Publisher Setup (One-Time)

In PyPI project settings for `efficient-agent-protocol`, add a Trusted Publisher with:

- Owner: `GenieWeenie`
- Repository name: `efficient-agent-protocol`
- Workflow name: `Release`
- Environment name: _(leave blank unless you add a GitHub environment gate)_

## Rollback Procedure

Use rollback only when a bad release is published.

1. Stop downstream rollout and communicate issue.
2. If package publication occurred:
   - publish a corrective follow-up release (`vX.Y.(Z+1)`) with fix
   - avoid force-overwriting historical release artifacts
3. If GitHub release/tag must be withdrawn:
   - delete the GitHub release entry
   - delete the bad tag locally and on origin
4. Document incident summary:
   - impact
   - root cause
   - corrective actions

### State Database Rollback

If the release introduced a schema migration that caused data issues:

1. Restore from the pre-upgrade backup (see `docs/state_backup_restore.md`).
2. Verify restored data with `scripts/verify_upgrade_from_baseline.py`.
3. Pin the previous package version until a fix is released.

## Post-Release Checklist

- CI release workflow succeeded.
- Release notes are accurate and follow `docs/release_notes_template.md` structure.
- Security scanning workflows remain green.
- Roadmap issue/task status updated.
- Post-release install smoke test passed (verified by release workflow).

## Security Workflow Notes

- `pip-audit` runs in CI and is always enforced on Python 3.11.
- Optional GitHub Dependency Review workflow can be enabled by setting repository variable:
  - `ENABLE_DEPENDENCY_REVIEW=true`
- GitHub Dependency Graph should be enabled for dependency-review support.
